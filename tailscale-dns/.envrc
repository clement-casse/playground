use flake

export BLOCKY_CONFIG_FILE="$(pwd)/blocky-config.yml";

if [ -f .env.secrets ]; then
    source .env.secrets
else
    echo 'export FLY_ACCESS_TOKEN="FlyV1 YOUR_SECRET_HERE";' > .env.secrets
fi